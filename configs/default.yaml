runtime:
  device: cuda
  mixed_precision: true
  batch_size: 512
  edge_backend: dexined
  charset: balanced
  smoothing: true

render:
  columns: 120
  fps: 24.0
  overlay_mode: ascii_only

training:
  cell_width: 8
  cell_height: 12
  fonts:
    - DejaVuSansMono.ttf
