runtime:
  device: cuda
  mixed_precision: true
  batch_size: 256
  edge_backend: dexined
  charset: dense
  smoothing: true

render:
  columns: 160
  fps: 24.0
  overlay_mode: blended

training:
  cell_width: 10
  cell_height: 16
  fonts:
    - DejaVuSansMono.ttf
